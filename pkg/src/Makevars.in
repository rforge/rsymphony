SYMPHONY_LIBS = -lSym -lCgl -lOsiClp -lClp -lOsi -lCoinUtils

PKG_CPPFLAGS = @SYMPHONY_INCLUDE_PATH@
PKG_LIBS = @SYMPHONY_LIB_PATH@ $(SYMPHONY_LIBS)

all: @SYMPHONY_TS@ $(SHLIB)

SYMPHONY.ts:
	@(cd SYMPHONY && make && make install)
	touch $@
